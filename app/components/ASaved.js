// Include React
import React from "react";

// Create the Saved Component

var Saved = function(props) {
    return (
        <div>{props.articles}</div>
    );
};

// Export the component back for use in other fi
export default Saved;